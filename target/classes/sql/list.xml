<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="list">
	<select id="myquestion" resultType="hashmap">
		select title, contents, substr(date, 1, 10) as date, answer, answercheck from qna where del_yn = 'y'
	</select>
	
	<select id="adminquestion" resultType="hashmap">
		select title, contents, id, substr(date, 1, 10) as date, answer, answercheck from qna where del_yn = 'y'
	</select>
</mapper>